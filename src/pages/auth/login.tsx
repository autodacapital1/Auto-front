import React from "react";
import { toast } from "sonner";

export function Login({ className, ...props }: React.ComponentProps<"div">) {

  // Define a função 'handleSubmit' que recebe um evento de formulário.
  // Previne o comportamento padrão do formulário.
  // Define a variável 'target' como o evento de formulário.
  // Chama a função 'login' com o email e a senha do formulário.
  // Define as variáveis 'login', 'loading', 'error' e 'success' com o hook 'useLogin'.
  return (
    <>
      <p></p>
    </>
  );
}
